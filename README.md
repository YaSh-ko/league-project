# ✅ Todo App (React + TS)

Простое и удобное SPA для ведения списка задач: просмотр, поиск, фильтрация, сортировка, создание, редактирование и удаление. Работает с REST API `/tasks` — можно подключить свой сервер или поднять быстрый мок.

## 📌 Описание
Приложение позволяет:
- видеть список задач с поиском и фильтрами по важности/статусу;
- создавать новые задачи и редактировать существующие;
- отмечать важные и завершённые задачи;
- получать быстрые уведомления о статусе операций (RTK Query + Snackbar);
- работать в едином лейауте с темной/светлой темой.

## 🛠️ Стек
- React 18, TypeScript
- React Router 6
- Redux Toolkit + RTK Query
- Material UI + Emotion
- Webpack 5, Babel, PostCSS/Sass

## ⚡ Особенности
- Поиск и фильтры без перезагрузки страницы.
- Оптимистичные обновления и кеширование запросов через RTK Query.
- Удобные формы создания/редактирования задач.
- Лёгко заменить или подключить свой бэкенд (описано ниже).

## 🚀 Установка и запуск
1) Установить зависимости:
```bash
npm install
```
2) Создать `.env` и указать адрес API:
```
NODE_ENV=development
APP_BASE_API_URL=http://localhost:3001
```
3) Поднять совместимый сервер (описание ниже).
4) Запустить фронтенд:
```bash
npm run dev
```
Открыть http://localhost:3000 (или адрес dev-сервера).

## 🌐 Ожидаемый сервер
Клиент обращается к `APP_BASE_API_URL` и ресурсу `/tasks`:
- `GET /tasks?name_like=&isImportant=&isCompleted=` — список с фильтрами.
- `GET /tasks/{id}` — получить задачу.
- `POST /tasks` — создать задачу.
- `PATCH /tasks/{id}` — обновить задачу.
- `DELETE /tasks/{id}` — удалить задачу.

Модель задачи (пример):
```json
{
  "id": 1,
  "name": "Позвонить клиенту",
  "info": "Созвон в 15:00",
  "isImportant": true,
  "isCompleted": false
}
```

Где взять сервер: реализуйте эндпоинты выше в своём бэкенде и пропишите его адрес в `APP_BASE_API_URL`.

Совет: не коммитьте реальные адреса/токены — `.env` хранится локально.

## 🔧 Скрипты
- `npm run dev` — dev-сервер с HMR.
- `npm run build` — production-сборка в `dist/`.
- `npm run watch` — сборка в режиме watch.
- `npm run lint:ts` / `npm run fix:ts` — линтинг/автофикс TS/TSX.
- `npm run lint:styles` / `npm run fix:styles` — линтинг/автофикс стилей.

## 🌱 Переменные окружения
Все клиентские переменные начинаются с `APP_`.
- `APP_BASE_API_URL` — базовый URL REST API (например, `http://localhost:3001`).

## 🗂️ Структура (основное)
- `src/App.tsx` — маршрутизация и провайдеры темы/уведомлений.
- `src/app/*` — страницы списка, добавления и редактирования задач.
- `src/api/todosApi/` — RTK Query endpoints для `/tasks`.
- `src/components/` — Layout, формы, карточки задач и пр.
- `src/hooks/` — фильтры, сортировка, операции с задачами.

## 📸 Скриншоты
Добавьте изображения в `docs/screenshots/` и вставьте ссылки:
```
![Список задач](docs/screenshots/list.png)
![Создание задачи](docs/screenshots/create.png)
```

## 📬 Контакты
- Email: yasha.kozhaev1.@mail.ru
- GitHub: https://github.com/YaSh-ko
